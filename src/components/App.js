import React, { Component } from 'react';

import OptionsMenu from './options_menu';

class App extends Component {
  render() {
    return (
      <div>
        <h1>Styled Components Demo</h1>
        <h4>See different features of Styled Components</h4>
        <OptionsMenu />
      </div>
    );
  }
}

export default App;
